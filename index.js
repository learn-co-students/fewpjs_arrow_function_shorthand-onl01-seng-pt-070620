let divide = function () {return 2000 / 100}

//written this way, needs return, will not do auto like arrow

let square = x => x ** 2;

//left of the arrow is variable set, to the right is function body

let add = (a, b) => a + b; 

//when you have two variables, need parenthesis
